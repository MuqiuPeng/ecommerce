import React from 'react';
import StoreCards from '@/components/ui/cards/store-cards';

export default async function StoreCardsPage({ userId }: { userId: string }) {
    return (
        <StoreCards />
    )
};